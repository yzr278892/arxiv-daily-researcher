# 更新日志

所有值得关注的版本变更均记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

---

## 🚀 v4.2 — 2026-09-01

### ✨ 新功能

1. **独立运行配置与安全迁移** — 主配置统一使用 Git 忽略的 `runtime/config.json`；v4.1 及更早版本的 `configs/config.json` 会在首次启动时安全复制并保留原文件。WebDAV 继续使用稳定的 `configs/config.json` 归档名，已有备份可直接恢复。
2. **历史维护调度** — 读取旧历史、历史数据补全和遗漏扫描共享“闲时运行”或“指定时间段”两种调度方式，默认时间段为 00:00–06:00；历史维护的单次论文上限与每日研究上限独立配置。
3. **自动收藏** — 可在收藏页启用“自动收藏通过论文”，并可手动扫描现有合格论文。收藏结果持续写入 SQLite，供收藏视图与偏好学习使用。
4. **通知测试与统一模板** — 邮件、企业微信、钉钉、Telegram、Slack 和通用 Webhook 均可从 WebUI 发送测试通知；每日研究、趋势任务、历史维护、补充报告和过去日报使用统一的平台化结果模板与失败摘要。
5. **报告批次导航** — 报告预览按实际报告批次前后跳转，不再按自然日期推断；同日的补充报告、过去日报和普通日报均可逐份浏览。
6. **部署文件分层** — 根目录 Compose 固定使用与 Release 一致的 GHCR worker/WebUI 镜像；`tests/docker-compose.yml` 专供本机源码构建与开发验证，避免将测试镜像作为日常运行部署。

### 🐛 修复与优化

1. **WebUI 性能与局部刷新** — 配置与状态读取加入缓存，长日志采用尾部读取，报告预览延后到目录可用后加载；设置、任务状态、检索和用量统计切换优先局部更新，避免跳回顶部或整页闪烁。
2. **界面与国际化完善** — 补齐动态状态、展开区、密钥占位提示和输入框的中英文文案；统一原生下拉箭头、分页、间距、代理范围与标签式 no-proxy 编辑器，修复高频控件悬停和布局问题。
3. **远程面板访问与账户安全** — WebUI 明确发布 `8501:8501`，适合受控 LAN 或 Tailnet 访问；支持受限 Tailnet 来源、管理员账户和会话保护。
4. **数据库恢复与容器权限** — 运行任务持有共享数据库活动锁，恢复操作在任务写入期间立即拒绝；Worker 与 WebUI 共同挂载 `runtime/` 并按 `PUID` / `PGID` 写入，防止新部署生成 root 所有文件。
5. **回归覆盖扩展** — 增加运行配置迁移、历史调度、自动收藏、通知测试、动态 i18n、局部刷新、报告导航和 Compose 运行语义的回归验证。

---

## 🚀 v4.1 — 2026-08-30

### ✨ 新功能

1. **现代管理 WebUI** — 主分支默认使用基于 Starlette/Uvicorn 的管理界面。侧栏按运行、内容、配置、系统分组，顶部保留 18 个页面；支持中英文、浅色/深色主题、管理员账户、7 天会话和可信内网跳过登录。
2. **任务与历史维护界面** — 每日研究、过去日报和趋势任务各自展示运行状态、最新日志和队列；历史维护独立管理旧版本导入、数据补全、遗漏扫描、问题摘要和重试入口。页面交互优先局部更新，避免刷新整个面板。
3. **旧历史导入可靠性** — 默认导入只登记旧 HTML 中已经交付的论文。完整修复可读取兼容 JSON，并为同一 HTML 论文以只读方式补齐旧 `keywords.db` 中缺失的关键词；SQLite 负责后续字段补全、遗漏扫描、自然周补充报告和可重试积压。
4. **多来源历史归并** — 每日研究、过去日报和遗漏扫描覆盖已启用来源。SQLite 以稳定身份归并同一论文，保留来源变体和无法合并的分析结果，关键词等可合并字段去重保存。
5. **评分与趋势配置** — 主关键词和作者加分支持逐项权重；趋势研究提供可编辑的内置提示词模板，综合分析固定使用所选模板。关键词标准化可选择 LLM 角色，趋势视图与报告频率可单独配置。
6. **运行诊断与用量统计** — 运行诊断、LLM 健康和来源健康支持 3/7/14/30 天或全部范围、按页查看和阶段错误摘要；用量统计提供热力图、横向汇总表、模型统计，以及 24 小时到自定义区间的 Token 折线图。
7. **运维与安全** — Worker 与 WebUI 以配置的 `PUID` / `PGID` 写入挂载目录；健康检查覆盖服务与共享状态；本地备份支持当天最大副本数、历史按日保留和可配置保留期，WebDAV 保持内容变化时上传的增量归档。

### 🐛 修复与优化

1. **LLM 兼容性检测** — OpenAI 兼容网关返回 404/405 时记录 Chat Completions / Responses 端点能力，后续调用跳过已确认不支持的无效回退；错误日志与通知提供脱敏的阶段摘要。
2. **任务协调与可观测性** — 历史导入在每日研究、过去日报、趋势任务和维护任务空闲后自动执行；触发与 worker 重启状态文件按上限轮转，失败状态展示阶段错误而非单一退出码。
3. **面板交互与可读性** — 报告预览去除加载虚线框；收藏论文改为表格；日志、报告、长列表和关键词采用边界明确的滚动或分页视图；PDF 解析器与关键词趋势设置调整控件间距。
4. **Docker 发布与端口** — 运行期移除 Streamlit 依赖，WebUI 镜像仅包含 ASGI 服务；首次健康检查会初始化必要 SQLite 路径；管理面板恢复为仅监听 `127.0.0.1:8501`。

---

## 🚀 v4.0 — 2026-08-25

### ✨ 新功能

1. **SQLite 日报历史重构** — SQLite 完全取代旧 `data/history/*_history.json` 作为每日研究历史（不迁移、不双写）。每篇论文持久化阶段状态、输入指纹、重试次数与完成时间；同一 `(source, canonical_id, version)` 只交付一次，arXiv `v1/v2/v3` 作为独立版本分别评分、翻译并可重新推送，新版本报告显示上一已推送版本及时间。
2. **持久化待处理队列** — 抓取候选先原子写入 SQLite 队列再处理；`max_papers_per_run` 默认 200，防止首次部署的大批量历史论文一次涌入（堆积由后续运行消化，消化完每天正常处理全部新论文），`0` 表示不限；正数仅限制本次评分/翻译/分析数量，超出部分留队下次优先，失败/重试论文优先于普通积压，WebUI 可调。
3. **完整抓取与扫描收据** — arXiv 同时扫描首次提交与最后更新并完整分页，不以任何预算截断；每个来源必须写入终态扫描收据，任一来源失败则本次运行明确失败，水位线只在完整运行提交时推进。
4. **声明式额外数据源** — 核心来源收敛为 arXiv 与 PRL，其余来源（PRA/PRB/Nature/Science、Hugging Face Papers）改为默认关闭的声明式 JSON 定义；严格拒绝 import path / Python / callback 等可执行字段。报告查看页自动发现任意来源目录，无需为每个来源修改 UI。
5. **Hugging Face Papers 源** — 新增可靠抓取与跨源 arXiv 镜像去重。
6. **只读评分诊断** — 评分审计保留非敏感证据，`scoring_evaluation diagnose` 提供分数漂移、扫描健康与发件箱诊断；原规划的人工标注反馈闭环按产品决策移除，由收藏偏好与学习模式评分承接。
7. **全文 TLDR 溯源** — 报告中标注 TLDR 是否来自全文解析。
8. **WebUI 运行可观测性** — 安全的运行观测、代理默认关闭（保留配置值）、每日推送面板提供 `0 = 全部` 的处理上限；运行诊断精简后并入「数据分析」页。
9. **LLM Token 用量统计** — 每次运行（含成功/失败/中断）结束时把 per-model token 用量持久化到 SQLite；「数据分析」页顶部常驻当日输入/输出与近 30 天累计汇总，下方为近一月 GitHub 风格热力图（默认滚动到最新日期）与静态自适应折线图（7/30/90/365 天/全部，坐标随数据自适应、不可手动缩放）+ 按模型汇总。数据永久保留。
10. **收藏偏好（融入报告查看页）** — 报告预览下方列出该报告当日交付的论文，可一边查看报告一边一键喜欢/不喜欢/清除标记，兴趣画像折叠展示。标记写入 SQLite（清除只写状态不删行，永久存档）。标记仅用于画像汇总与学习模式信号，**不影响其余评分策略**。
11. **WebUI 页面结构重组（顶部 Tab）** — 保持顶部 Tab 点击切换；页面为 12 个：「收藏与检索」独立成页（位于报告查看与趋势分析之间），用量统计/数据源健康/运行诊断合并为「数据分析」页、网络代理并入「高级设置」页。保存未浏览页面时保留磁盘现值，不会被默认值覆盖。
12. **运行监控实时化** — 运行状态与活跃日志尾部每 5 秒自动刷新（`st.fragment`，可开关）；每日推送页常驻"待处理队列 / 失败待重试"指标。
13. **停止运行** — 运行中可从面板经二次确认发送停止信号（共享卷 + worker 侧监听转发 SIGTERM），已完成阶段保留、未完成论文留队，状态记录为 `interrupted`。
14. **触发状态真实化** — 相同任务已在运行时，触发改记为 `skipped_busy`（专用退出码 75）而不再伪装成成功；趋势分析启动按钮加防重复守卫。
15. **数据库备份** — 每日运行结束后自动对 SQLite 数据库做 gzip 压缩备份（SQLite backup API 一致性快照）：本地为全量备份，按文件时间戳自动删除超过保留期的副本（WebUI 可配置，默认 7 天；填 0 永久保留）；WebDAV 为增量备份，仅当数据库内容较上次上传发生变化时上传，远端副本永不删除（永久存档）。上传走压缩文件节省流量，WebDAV 失败不影响本地备份。WebUI 数据管理页提供开关、保留天数与"立即备份"。
16. **论文检索（报告查看页最下方）** — 基于数据库元数据的全量历史检索：标题/作者/摘要/TLDR/提取关键词匹配 + 来源、处理日期范围、最低总分、只看收藏过滤，分页浏览；数据永久存档，存量随时间增长也不怕找不到。
17. **数据源健康（数据分析页）** — 每个数据源最近 20 次扫描收据聚合：最近状态与时间、成功率、最近新增候选数、最近一次错误，arXiv 主源置顶展示；运行诊断精简为"近 10 次运行 / 及格率 / 通知积压 + 最近一次扫描收据"。
18. **学习模式评分（`learned_preference_v1`）** — 第三种评分策略：在 v1 加权总分之上叠加学习库修正。学习库由收藏/不喜欢（强信号 ±1）与 v1 及格历史（弱信号 +0.25）持续累积为带权重的关键词/作者库；单项权重先限幅（默认 ±2.0）再整体衰减（默认 ×0.5），学习项影响始终低于直接配置的评分关键词，直接配置的关键词不重复计权。信号按 (论文, 词, 类型) 幂等 upsert、永不删除；评分页可调衰减/限幅并预览学习库。
19. **趋势分析自定义提示词** — 「分析技能」仅保留默认勾选的综合分析；新增深度分析提示词框，非空时替换综合分析内置指令（运行请求即时透传，也随配置持久化），支持保存/应用/删除提示词模板。
20. **关键词页改版** — 研究背景置顶；参考文献 PDF 关键词提取区新增"已提取关键词"只读展示框（按权重排序）。
21. **额外来源下拉选择** — 额外来源启用后才展开配置；内置来源（PRA/PRB/Nature/Science/Hugging Face Papers 等）改为多选下拉，自定义来源通过表单校验新增，移除 JSON 文本框与可复制模板。
22. **报告版本标识精简** — 论文标题旁不再标注 vN 与「↻ 重试」运维标记；版本只在详情字段（Version: vN，HTML/Markdown 一致）展示，修订版（🔁）标签保留，显示层同步过滤历史报告中的遗留徽标。
23. **收藏与检索独立 Tab** — 「报告查看」与「趋势分析」之间新增页面：收藏论文按时间排列（标题超链接直达 arXiv，无 URL 时回退 `arxiv.org/abs/<id>`）、👍/👎 统计、收藏关键词频次与高产作者统计；论文检索从报告查看页迁入本页。
24. **ArXiv 全分类下拉** — 「搜索与数据源」与「趋势分析」两处的 ArXiv 分类过滤改为内置全部 153 个一级分类的下拉多选（按字母序、显示「代码 · 名称」），移除「其他自定义分类」文本框。
25. **每日运行时间面板化** — 「每日研究设置」新增时间选择器，写入 `daily_research.run_time`（HH:MM，默认 12:00）；调度完全由 config.json 驱动（容器启动时解析并安装 cron），移除 `CRON_SCHEDULE` 环境变量覆盖；侧边栏新增「重启主研究容器」按钮（共享卷标记 + worker 归档后向 PID 1 发 TERM，约 5 秒完成重启并重装 cron）。
26. **关键词标准化静默定时化** — LLM 批量关键词标准化与趋势报告从日报主流程拆出，改为每日 0 点由独立 cron 任务（`modes/keyword_maintenance`）静默执行：输出仅入 `logs/keyword_*.log`，失败不影响日报交付、次日自动重试；支持运行锁与按频率生成报告。
27. **长任务进度反馈** — 每日研究各阶段（准备/抓取/评分翻译/深度分析/生成报告）向 SQLite 写入阶段心跳，「每日推送」页实时显示当前阶段、登记/已评分/已分析/交付/失败计数、运行时长与评分/分析进度条（5 秒自动刷新）；心跳在 run 终态时清理、终态后拒绝写入，心跳缺失时按论文阶段状态推断兜底。
28. **LLM 超时与重试统一强化** — 所有 OpenAI 客户端共享统一边界（`llm` 段配置：单请求超时默认 300s、SDK 快速重试、应用层 5 次尝试、5–120s 指数退避带抖动）；429/5xx/超时/空响应按瞬态重试，401/403/404/400 等认证参数错误快速失败；配合全局请求池限速适配低并发低 TPS 中转。
29. **arXiv 退避统一** — 领域扫描与关键词搜索共享同一套退避策略：超时/一般错误线性退避（30–90s）、限流指数退避（60–480s）、遵从响应头 Retry-After（封顶 600s）、领域重试耗尽后跨领域冷却 60s；抓取超时改为"无进展"看门狗（持续收到结果即合法延长，避免大时间窗误杀）。
30. **报告标记无闪屏** — 👍/👎 按钮移至卡片内与评分同行（右侧）；点击走乐观更新 + postMessage 回传落库，报告 iframe 不重建、页面无局部刷新；标记状态经组件参数单独下发，与报告 HTML 解耦。
31. **关键词权重三档展示与默认解析器** — 参考文献关键词权重分布改为高/中/低重要性三个边框分组（带 emoji、去斜体）；「资格与排序策略」更名「评价策略」；PDF 解析默认切换为本地 PyMuPDF。
32. **旧版本历史导入** — WebUI「数据管理 → 数据库备份」下方新增「读取旧历史」：默认只把 v3.2 HTML 日报中已有的 arXiv 卡片写入 SQLite 交付账本，避免升级后重复推送。开启完整修复后，兼容 JSON 与 `keywords.db` 同步迁移；重复分析按最新报告覆盖，v4 已有行不会被降级，整批导入可重复执行且幂等。
33. **历史数据补全与时间段扫描** — 完整修复以及独立维护任务均以 SQLite 论文记录和交付账本为依据：缺失 TL;DR、译文或深度分析会补全并回写原报告；遗漏扫描按 SQLite 覆盖的 arXiv 时间范围分块执行，按月查询、跨领域去重并记录待处理项。
34. **自动补充报告** — 完整修复完成后会把无报告兼容数据和遗漏论文自动衔接到补充流程；遗漏以 ISO 自然周分组，格式与每日报告一致，显示于「报告查看 → 每日研究」并按时间戳排序。单次处理篇数受「每日推送 → 本次最多处理论文数」约束，未完成项留在 SQLite 积压中供后续维护任务重试。
35. **过去时间段每日报告** — 「每日推送」新增过去日期选择器，等价于在那个日期当天跑一次每日研究：只抓取该日新论文、走完整评分/翻译/分析流程，生成当日日报；报告文件名时间戳 = 过去日期 + 本次实际运行时分秒，因此与历史真实报告一起按时间线排布，不与今天的报告混淆。
36. **报告时间线排序** — 报告文件名时间戳精确到微秒，「报告查看 → 每日研究」按文件名时间戳（而非文件修改时间）排序，同日多份报告（含补充报告、过去日报）次序稳定。
37. **日报固定 3 天窗口** — 移除「搜索最近 N 天」配置：每日报告固定回看最近 3 天并处理窗口内全部新论文（扫描水位线恢复机制不变），更早日期的新论文由「过去时间段每日报告」补跑；旧配置文件中的 `search_days` 兼容读取但忽略。

### 🛡️ 可靠性加固

1. **fail-closed 状态管理** — 状态损坏、无效配置、空扫描范围、畸形 OpenAlex 元数据等一律显式失败，不把部分结果伪装成完整日报。
2. **原子交付** — 报告文件落盘后，论文交付、运行完成、通知 outbox 与维护任务在单个 SQLite 事务中提交；通知/WebDAV 失败不会导致论文重复推送。
3. **边界加固** — WebDAV 恢复限制在受校验文件、Webhook 边界收紧、外部 PDF 下载受限与校验、导入配置路径限制在项目内、动态 Markdown 消毒、报告预览隔离。
4. **进程与运行安全** — 运行锁跨重启安全、管道失败传导至进程退出码、日志不可写时保持运行存活。

### 🐛 Bug 修复 / 优化

1. **Docker 镜像拆分与安全修复** — Worker 与 WebUI 依赖分离（`requirements-core.txt` / `requirements-webui.txt`），Worker 不再安装 Streamlit 全家桶；WebUI 面板默认仅绑定 `127.0.0.1:8501`；cron 不再把容器全部环境变量及密钥复制到 `/etc/environment`。
2. **版本比较修复** — 更新检查改用语义化版本比较：远端更旧（如本地开发版 4.0 vs 已发布 3.2）不再误报"发现新版本"；同一远端版本只通过通知渠道提醒一次。
3. **配置注释保留** — WebUI 保存不再清洗 `config.json` 里的手写注释：注释块按锚点键回注到新文件，生成式分节横幅不会重复，已删除键的注释自然丢弃。
4. **Docker 挂载 `.env` 保存修复** — 单文件 bind mount 上 `os.replace` 返回 EBUSY 导致 WebUI 无法保存 `.env`；现在回退为原地覆写，inode、宿主属主与权限保持不变。
5. **趋势分析本地启动修复** — 非 Docker 启动路径补上缺失的 `import time`，成功启动不再立即 NameError。
6. **镜像统一多阶段构建** — 单个 `docker/Dockerfile` 提供 `worker`/`webui` 两个构建目标，共享 slim 基底与时区/公共依赖层，构建后剥离 `__pycache__`；worker 不再携带 `src/webui`，面板不再携带未使用模块。实测 worker 384→346MB、WebUI 785→692MB；遗留的根目录 `requirements.txt` 与 `Dockerfile.webui` 移除。
7. **arXiv 公告延迟重扫**、**陈旧处理阶段失效**、**期刊身份在 arXiv 增强后保留**、**评分输出校验**、**LLM 响应规范化与深度分析校验**、**重试时不丢失可选 Semantic Scholar 增强** 等数十项修复。

### 🔧 收尾增强

1. **大型工作流统一通知** — 旧历史导入（含自动补充）、手动补充报告、过去日报范围队列、趋势研究和日常研究均通过 SQLite outbox 发送一个汇总结果；部分完成、失败或延后会包含具体阶段/问题摘要而非完整日志。
2. **旧历史导入可靠性验收** — 补齐空闲等待互斥、重复分析取最新、缺失数据留队重试、导入后自动补充报告、过去日报逐日持久队列的回归测试与运行状态呈现。
3. **备份留存精化** — 本地当天保留全部快照；昨天及更早每一天仅保留最新快照，再套用可配置保留期（默认 7 天，`0` 永久保留）。WebDAV 仍为仅内容变化时上传、远端永不删除的增量归档。
4. **数据源/API 面板完善** — arXiv 与额外来源采用独立开关和折叠配置；PRL 归入内置额外来源；OpenAlex/Semantic Scholar 仅在启用时调用，分别补齐测试连接、官方控制台链接与策略说明；移除已废弃的 OpenAlex 联系邮箱。
5. **PDF 与关键词界面一致性** — 选择 PyMuPDF 时隐藏 MinerU 设置；关闭参考文献关键词提取后不显示也不使用缓存关键词；长列表统一在超过 10 条时使用原生滚动容器。
6. **评分体验本地化** — 三种评分策略在选择器中使用中英本地化名称，策略说明改为“资格判断 + 排序信号”的简洁完整表述；V2 明确禁止参考词/作者让无关论文通过。
7. **被动 LLM 健康面板** — 「数据分析」新增 CHEAP_LLM / SMART_LLM 健康卡，仅记录真实调用重试后的终态，不发探针、不额外消耗 token；展示最近结果、连续失败、近 20 次成功率、最近成功和脱敏错误说明。
8. **Streamlit 兼容升级** — 全部 `use_container_width` 调用切换为当前 `width="stretch"` API，消除弃用警告；运行日志查看区固定为 800px 并原生滚动。
9. **更新检查边界明确** — Docker 环境只检查 GitHub Release 并发送一次性新版本通知，不会自动拉取、重建镜像或重启容器。
10. **发布链路生产化** — GitHub Actions 的每日/趋势工作流迁移到 v4.0 依赖与 SQLite 状态缓存，移除废弃的搜索天数输入并串行化共享状态；新增主分支完整回归，以及匹配 `VERSION` 的 `v<版本>` tag 才会在回归成功后发布 AMD64/ARM64 双架构 GHCR worker 和 WebUI 镜像。
11. **历史维护分层** — “读取旧历史”默认只将旧 HTML 中已存在的 arXiv 卡片写入 SQLite 交付账本，避免升级后重复推送且不调用 LLM。开启“完整修复旧历史”后才迁移兼容 JSON 与 `keywords.db`、补齐缺失字段、回写原报告并扫描遗漏；“历史数据补全”和“历史遗漏扫描”也可从数据管理页独立运行。
12. **SQLite 历史权威化** — 缺失字段检查、报告回写和遗漏范围扫描均以 SQLite 论文记录与交付账本为依据，HTML 只在初次导入时解析。遗漏论文按 ISO 自然周分组，并受单次最大处理数限制拆分为多份补充报告。
13. **旧关键词库兼容迁移** — 完整修复可只读迁移 v3.2 `keywords.db` 的逐论文关键词、原始日期、规范词与别名；NAS 只读挂载支持安全快照读取，存在未合并 WAL 时明确提示，避免静默遗漏数据。
14. **历史任务可观测性** — 历史导入、字段补全和遗漏扫描均写入阶段心跳、逐阶段日志、SQLite 终态与通知摘要。内部步骤失败会标记父任务失败，同时保留精确的待重试条目。
15. **LLM 端点能力检测** — 对 OpenAI 兼容网关记录已确认不支持的 Chat Completions / Responses 端点。发现 `/responses` 404/405 后持久化该能力并停止无效回退与重试，日志只保留脱敏原因。
16. **旧报告标记兼容** — 报告查看预览可直接为 v3.2 HTML 注入持久化 👍 / 👎 控件，即使用户尚未执行旧历史导入；归档 HTML 文件保持不变。

### 📚 文档与验证

1. **README 结构化重整** — 中英文 README 延续 v3.2 的功能分组、导航表、配置/部署表、流程图表、复杂 FAQ 与版本摘要版式，并以 v4.0 当前实现重写 SQLite、队列、历史导入、过去日报、通知、备份、评分、部署与故障排查内容。
2. **最新脱敏 WebUI 截图** — 更新每日推送、数据分析、评分、备份和旧历史导入界面截图，不包含 API Key、密码、Webhook、邮箱、内网地址或本机路径。
3. **回归验证** — 完整测试套件通过：`492 passed, 4 warnings, 64 subtests passed`；worker 与 WebUI Compose 容器均通过健康检查。
4. **托管镜像部署说明** — Compose 支持通过 `ADR_WORKER_IMAGE` 与 `ADR_WEBUI_IMAGE` 显式固定 GHCR 镜像版本，同时保留默认的本地源码构建方式。

---

## ✅ v3.2 — 2026-04-26

### ✨ 新功能

1. **网络代理系统** — 支持 HTTP / SOCKS5 代理，可按服务粒度控制（ArXiv / OpenAlex / Semantic Scholar / LLM API / 通知 / 检查更新），WebUI 提供完整的代理设置面板。
2. **WebDAV 数据同步** — 按需同步配置和数据文件到 WebDAV 服务器（坚果云、NextCloud 等），支持手动 / 定时 / 报告后自动三种同步模式。内置坚果云 HEAD 请求不兼容的修复（使用 PROPFIND 替代），同步范围可选配置文件、历史记录、关键词数据、报告。
3. **配置一键导出** — WebUI 数据管理面板支持一键将 `config.json` 和 `.env` 打包为 zip 下载，兼容 Docker 卷挂载路径。
4. **Docker 版本更新通知** — Docker 环境下通过 GitHub API 检查最新版本，发现新版本时通过已配置的通知渠道推送更新提醒。
5. **通知模板外部化** — 更新通知使用 `configs/templates/` 下的独立模板文件，支持按平台自定义格式，加载失败时自动回退到内置格式。
6. **每日推送 Tab** — 原「运行管理」Tab 重组为「每日推送」，新增「每日研究设置」区域，集中管理 HTML 报告、Markdown 报告、包含所有论文三个开关，评分 Tab 中的「包含所有论文」开关同步迁移至此。
7. **Markdown 报告独立开关** — 新增 `enable_markdown_report` 配置项，Markdown 和 HTML 报告可独立启用/关闭，在「每日推送」Tab 中直接控制。
8. **趋势分析输出格式双开关** — 输出格式从多选下拉框改为两个独立 Toggle（Markdown / HTML），默认均开启，在「趋势分析」Tab 中操作。
9. **运行日志刷新自动跳转** — 「每日推送」Tab 的日志刷新按钮改为自动打开最新的非系统日志，无需手动选择。
10. **高级设置重组** — 原「报告」分区重命名为「功能开关」，HTML 报告 Toggle 移出，仅保留 Token 追踪和自动更新检查。

### 🐛 Bug 修复 / 优化

1. **WebDAV 坚果云兼容修复** — 坚果云不支持 HEAD 请求（返回 403），webdavclient3 的 `check()` 方法依赖 HEAD 导致所有连接操作失败。传入 `disable_check=True` 并改用 PROPFIND (Depth 0) 进行存在性检查，彻底修复。
2. **ArXiv 抓取逻辑优化** — 内部请求量扩大 3 倍以覆盖更多新论文；新增连续已处理论文早停机制（阈值 50 篇），提高效率。
3. **Docker Compose 位置调整** — `docker-compose.yml` 从 `docker/` 目录移至项目根目录，简化 Docker 命令。
4. **Docker 镜像优化** — 移除未使用的系统依赖，精简镜像体积。
5. **配置导出路径修复** — 修复 Docker 卷挂载环境下配置导出显示"未找到可导出文件"的问题，增加多路径候选检测。
6. **每日深度分析可配置** — 新增 `daily_research.enable_deep_analysis` 配置项，可独立控制每日研究模式是否执行 PDF 深度分析。
7. **Telegram 通知 HTML 渲染修复** — 修复 Telegram 通知中 HTML 标签未被正确渲染的问题。

### 📦 架构变更

1. **WebUI 面板风格统一** — 所有 Tab 内分节标题统一为 `section-title` 样式（粗体蓝色下划线 + 语境化 emoji）。
2. **配置系统扩展** — `config.json` 新增 `proxy.scope.update_check` 和 `report_settings.enable_markdown_report` 字段。
3. **趋势分析面板优化** — 运行控制按钮移至 Tab 顶部，关键词输入紧跟其后，提升操作效率。

---

## ✅ v3.1 — 2026-04-15

### ✨ 新功能 / 重构

1. **运行管理 Tab 完全重构** — 统一本地模式与 Docker 模式的立即运行体验；支持停止任务、查看状态、清理失效锁。
2. **日志查看器升级** — 日志区域按"系统日志 / 运行日志 / 其他日志"三栏分组，选中即展示，共享同一内容区；默认优先展示时间最新且非系统日志。
3. **趋势分析 Tab 完整双语化** — 趋势分析界面的参数、状态、按钮、Skill 标签等文本全部纳入 i18n。
4. **趋势分析配置收口到独立 Tab** — 趋势研究相关的排序、最大结果数、TLDR、输出格式与 Skill 选择统一放入「趋势分析」Tab。
5. **ArXiv 抓取超时守卫** — 新增 `data_sources.arxiv.fetch_timeout_seconds` 配置，使用硬超时保护单个领域抓取。
6. **运行锁超龄回收** — 对超过 `run_lock.max_age_hours` 的任务尝试自动终止旧进程并回收锁。
7. **报告查看增强** — 默认自动打开当前可见范围内最新的报告；刷新后重新跳转到最新可见报告。

### 🐛 Bug 修复

1. **报告日期导航修复** — 修复同一日期有多份报告时，前一天/后一天导航可能错误定位的问题。
2. **日志默认选择逻辑修复** — 运行管理页首次进入时，不再默认打开系统日志，优先显示最新的实际运行日志。
3. **i18n 条目补齐** — 新增并补齐运行管理、日志查看、趋势分析等相关翻译项。

---

## ✅ v3.0 — 2026-03-09

### ✨ 新功能

1. **研究趋势分析模式** — 全新 `trend_research` 运行模式，支持指定关键词和时间范围批量检索 ArXiv 论文，LLM 逐篇生成 TLDR，SMART_LLM 单次调用综合分析五个维度（热点话题、时间演变、核心研究者、研究空白、方法论趋势）。CLI 通过 `--mode trend_research --keywords "..."` 启动，支持 `--date-from`、`--date-to`、`--sort-order`、`--max-results`、`--categories` 参数。
2. **趋势分析 Skills 系统** — 五个分析维度合并为一个综合 Skill 单次调用，支持在 `config.json` 中配置启用的 Skill。
3. **研究趋势专属通知模板** — 新增趋势分析成功/失败通知模板（Markdown + HTML）。
4. **Token 消耗追踪** — 全局 Token 计数器，线程安全单例，对所有 LLM 调用无侵入式埋点，按模型统计输入/输出 Token 数。每次运行结束展示在报告和通知中。
5. **关键词趋势 HTML 报告** — 生成独立 HTML 趋势报告，使用 CSS 彩色水平柱状图、颜色编码图例表格和趋势热图。
6. **交互式配置向导 + Docker 自动触发** — 7 步 CLI 向导，Docker 首次部署自动运行。
7. **Streamlit 配置面板** — 浏览器配置面板，6 个配置 Tab 页覆盖所有配置项，支持 LLM 连接测试、SMTP 测试、实时计算评分预览。
8. **运行专用日志文件** — 每次运行自动创建独立日志文件：`daily_YYYYMMDD_HHMMSS.log` / `trend_YYYYMMDD_HHMMSS.log`。
9. **统一依赖管理** — WebUI 容器不再使用独立的 `requirements-webui.txt`，与主容器共用 `requirements.txt`。
10. **并发运行互斥锁** — 基于 `fcntl.LOCK_EX | LOCK_NB` 文件锁，防止同一任务被重复并发触发。daily_research 全局互斥，trend_research 按参数哈希独立互锁。
11. **Streamlit 报告查看器** — 三列并排展示全部类型报告文件，页内嵌入渲染 HTML。

### 📦 架构变更

1. **双模式 CLI 入口** — `main.py` 新增 `argparse` 命令行解析。
2. **报告目录重组** — 所有报告统一归入 `data/reports/`，按类型分目录。
3. **`modes/` 目录完整化** — 每日研究流水线提取为 `DailyResearchPipeline` 类。
4. **`report/` 目录按模式拆分** — 按运行模式重组为 `report/daily/`、`report/trend/`、`report/keyword_trend/`。
5. **配置管理基础模块** — 新增 `src/utils/config_io.py`，统一的 `.env` / `config.json` 双向读写能力。

---

## ✅ v2.3 — 2026-03-09

### ✨ 新功能

1. **邮件通知 HTML 精美模板** — 6 个模板（运行成功/失败 + 4 种错误告警）采用响应式卡片设计，内嵌 inline CSS 兼容主流邮件客户端。
2. **通知渠道独立开关** — 各渠道的 `enabled` 开关，可精细控制每个渠道的启用状态。

---

## ✅ v2.2 — 2026-03-03

### 📦 源代码结构重构

1. 按功能模块拆分 `src/` 目录为 `agents/`、`sources/`、`report/`、`notifications/`、`parsers/`、`keyword_tracker/`。

### 🐛 Bug 修复

1. 修复 JSON 转义修复器正则表达式失效
2. 修复并发模式下历史记录读取的竞态条件
3. 修复并发模式下 SQLite "database is locked" 错误
4. 修复并发模式下临时 PDF 文件名冲突
5. 修复 PDF 文件句柄泄漏
6. 修复 OpenAlex 分页数日志 Off-by-one
7. 修复合成 DOI 生成无效 URL
8. 修复关键词缓存目录未创建导致保存静默失败
9. 修复 `INSERT OR IGNORE` 后 `lastrowid` 不可靠
10. 修复参考 PDF 关键词分配逻辑
11. 修复 `git rev-list` 返回码未检查

---

## ✅ v2.1 — 2026-03-03

### 📋 许可证变更

1. 许可证从 CC BY-NC-SA 4.0 变更为 AGPL-3.0。

### ✨ 新功能

1. 通知消息 Markdown 模板系统
2. 运行时错误实时告警通知

---

## ✅ v2.0 — 2026-03-03

### 📦 项目结构重构

1. 源代码目录化（`src/`）
2. 配置文件集中管理（`configs/`）
3. 运行脚本集中管理（`scripts/`）
4. Docker 文件集中管理（`docker/`）
5. 精简 `.gitignore` / `.dockerignore`

### ✨ 新功能

1. 自动更新检查
2. Docker 一键部署
3. 多渠道通知系统（邮件、企业微信、钉钉、Telegram、Slack、通用 Webhook）
4. GitHub Actions 定时工作流
5. 自动重试机制（tenacity 指数退避）
6. 并发处理（ThreadPoolExecutor）
7. HTML 报告生成
8. MinerU 云端 PDF 解析
9. HTML 报告 CSS 外置
10. 报告目录结构优化
11. 关键词数据目录整理
12. 日志轮转管理
13. 按数据源单独配置搜索数量
14. Makefile 便捷命令
15. VSCode 工作区配置

---

## ✅ v1.3 — 2026-03-01

### 🐛 Bug 修复

1. 修复 OpenAlex 数据源每页只处理最后一篇论文的严重 Bug
2. 修复关键词标准化 JSON 解析失败
3. 修复关键词标准化 JSON 截断问题

---

## ✅ v1.2 — 2026-02-08

### 🐛 重要修复

1. 修复 OpenAlex 源缩进错误

### ⚡ 性能优化

1. 翻译缓存机制
2. KeywordTracker 实例优化

### ✨ 功能增强

1. ArXiv 优先策略
2. 增强 Semantic Scholar 集成

---

## ✅ v1.1 — 2026-02-05

### ✨ 运行脚本增强

1. 新增虚拟环境自动检测与创建
2. 新增 macOS 运行脚本
3. 新增 Windows PowerShell 脚本
4. 增强 Linux 运行脚本

---

## ✅ v1.0 — 2026-02-06

### 🎉 首次正式发布

1. 多数据源支持（ArXiv + 20+ 学术期刊，基于 OpenAlex）
2. 智能评分系统（关键词加权 + 动态及格线）
3. 深度 PDF 分析（LLM 驱动，PyMuPDF 本地解析）
4. 关键词趋势追踪（SQLite 存储 + Mermaid 可视化）
5. AI 关键词标准化（批量语义归并）
6. Markdown 报告生成（按数据源分目录）
